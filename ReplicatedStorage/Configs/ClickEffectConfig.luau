-- Configuration for click particle effects
-- Note: Visual properties (texture, color, size, etc.) are configured directly
-- in the Emitter2D Configuration attributes in StarterGui.MouseGui.Mouse.Emitter

return {
	-- Enable/disable automatic click effects
	Enabled = true,

	-- Number of particles to emit per click
	EmitCount = 4,
}
